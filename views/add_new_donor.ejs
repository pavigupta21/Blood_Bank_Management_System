<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Donor Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen flex justify-center items-center bg-zinc-800 text-white">
        <a href="/home" class="absolute top-4 left-4 text-blue-500 hover:text-blue-700 font-semibold">back to home</a>
        <div class="bg-zinc-900 p-8 rounded-lg shadow-lg w-[650px]">
            <h3 class="text-xl font-semibold text-center mb-6">Fill Donor Details</h3>
            <% if (typeof error !== 'undefined') { %>
                <div class="bg-red-600 text-white p-2 rounded mb-4 text-sm text-center"><%= error %></div>
            <% } %>
        
            <form action="/addnewdonor" method="post">
                <!-- Donor Details Section -->
                <div class="space-y-4 mb-6">
                    <div class="flex justify-between">
                        <div class="w-full mr-2">
                            <label class="block mb-1 text-sm">Donor Name</label>
                            <input type="text"  class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter donor's full name" name="name" value="<%= typeof name !== 'undefined' ? name : '' %>" required>
                        </div>
                        <div class="w-full ml-2">
                            <label class="block mb-1 text-sm">Age</label>
                            <input type="number" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter donor's age" name="age"  value="<%= typeof age !== 'undefined' ? age : '' %>" required>
                        </div>
                    </div>

                    <div class="flex justify-between">
                        <div class="w-full mr-2">
                            <label class="block mb-1 text-sm">Gender</label>
                            <input type="text"  class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter 'M'/'F'/'other'" name="gender" value="<%= typeof gender !== 'undefined' ? gender : '' %>"  required>
                        </div>
                        <div class="w-full ml-2">
                            <label class="block mb-1 text-sm">Contact</label>
                            <input type="number"  class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter donor's contact number" name="contact" value="<%= typeof contact !== 'undefined' ? contact : '' %>"  required>
                        </div>
                    </div>

                    <div class="flex justify-between">
                        <div class="w-full mr-2">
                            <label class="block mb-1 text-sm">Address</label>
                            <input type="text"  class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter donor's address" name="address" value="<%= typeof address !== 'undefined' ? address : '' %>" required>
                        </div>
                        <div class="w-full ml-2">
                            <label class="block mb-1 text-sm">Blood Group</label>
                            <input type="text"  class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Eg: AB+" name="blood_group" value="<%= typeof blood_group !== 'undefined' ? blood_group : '' %>" required>
                        </div>
                    </div>

                    <div class="w-full">
                        <label class="block mb-1 text-sm">Units Donated</label>
                        <input type="number"  class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Eg: 100" name="units_donated" value="<%= typeof units_donated !== 'undefined' ? units_donated : '' %>" required>
                    </div>
                </div>

                <!-- Medical Details Section -->
                <div class="space-y-4 mb-6">
                    <div class="flex justify-between">
                        <div class="w-full mr-2">
                            <label class="block mb-1 text-sm"> Have you ever tested positive for HIV/AIDS, Hepatitis B, or Hepatitis C?</label>
                            <input type="text" name="disease_status" value="<%= typeof disease_status !== 'undefined' ? disease_status : '' %>" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600" placeholder="If yes, please specify else 'No'">
                        </div>
                        <div class="w-full ml-2">
                            <label class="block mb-1 text-sm">Do you have a history of heart disease, high blood pressure, or diabetes?</label>
                            <input type="text" name="chronic_conditions" value="<%= typeof chronic_conditions !== 'undefined' ? chronic_conditions : '' %>" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600" placeholder="If yes, please specify else 'No'">
                        </div>
                    </div>

                    <div class="flex justify-between">
                        <div class="w-full mr-2">
                            <label class="block mb-1 text-sm">Are you currently taking any prescription or medications on a regular or temporary basis?</label>
                            <input type="text" name="medications" value="<%= typeof medications !== 'undefined' ? medications : '' %>" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600" placeholder="If yes, please specify else 'No'">
                        </div>
                        <div class="w-full ml-2">
                            <label class="block mb-1 text-sm">Do you have any chronic medical condition that requires ongoing treatment?</label>
                            <input type="text" name="ongoing_conditions" value="<%= typeof ongoing_conditions !== 'undefined' ? ongoing_conditions : '' %>" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600" placeholder="If yes, please specify else 'No'">
                        </div>
                    </div>
                </div>

                <!-- Submit Button -->
                <button class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded">Add Donor</button>
            </form>
        </div>
    </div>
</body>
</html>
