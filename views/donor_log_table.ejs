<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Donor Log Table</title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-zinc-800 text-white min-h-screen flex flex-col items-center justify-center px-4">
<a href="/home" class="absolute top-4 left-4 text-blue-500 hover:text-blue-700 font-semibold">back to home</a>
    <h1 class="text-3xl font-bold mb-8">Donor Log Table</h1>

    <div class="min-w-[600px] w-full max-w-md bg-zinc-900 rounded-lg shadow-lg">
        <table class="min-w-full text-center">
            <thead>
                <tr class="bg-zinc-700">
                    <th class="py-3 px-4">Entry id</th>
                    <th class="py-3 px-4">Donor id</th>
                    <th class="py-3 px-4">Donor Name</th>
                    <th class="py-3 px-4">Units Donated</th>
                    <th class="py-3 px-4">Date Of Donation</th>
                    
                </tr>
            </thead>
            <tbody>
                <% logtableData.forEach(item => { %>
                    <tr class="border-t border-zinc-600 hover:bg-zinc-700">
                        <td class="py-2 px-4"><%= item.entry_id %></td>
                        <td class="py-2 px-4"><%= item.donor_id %></td>
                        <td class="py-2 px-4"><%= item.name %></td>
                        <td class="py-2 px-4"><%= item.units_donated %></td>
                        <td class="py-2 px-4"><%= item.date_of_donation.toLocaleDateString('en-CA') %></td>
                        <td class="absolute right px-2 py-3 text-red-400 hover:text-red-600  group-hover:inline">
                            <a href="/deletedonorlog/<%= item.entry_id %>" class="text-sm font-medium">Delete</a>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>

</body>
</html>
