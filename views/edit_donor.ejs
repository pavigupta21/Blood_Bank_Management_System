<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Donor Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen flex justify-center items-center bg-zinc-800 text-white">
        <a href="/readdonors" class="absolute top-4 left-4 text-blue-500 hover:text-blue-700 font-semibold">go back</a>
        <div class="bg-zinc-900 p-8 rounded-lg shadow-lg w-96">
            <h3 class="text-xl font-semibold text-center mb-4">Update Donor Details</h3>
            <% if (typeof error !== 'undefined') { %>
                <div class="bg-red-600 text-white p-2 rounded mb-4 text-sm text-center"><%= error %></div>
              <% } %>
            <form action="/updatedonor/<%= donor.donor_id %>" method="post">
                <div class="mb-3">
                    <label class="block mb-1 text-sm">Donor Name</label>
                    <input type="text" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter donor's full name" name="name" value="<%=donor.name%>">
                </div>
                <div class="mb-3">
                    <label class="block mb-1 text-sm">Age</label>
                    <input type="number" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter donor's age" name="age" value="<%=donor.age%>">
                </div>
                <div class="mb-3">
                    <label class="block mb-1 text-sm">Gender</label>
                    <input type="text" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter 'M'/'F'/'other'" name="gender" value="<%=donor.gender%>">
                </div>
                <div class="mb-3">
                    <label class="block mb-1 text-sm">Contact</label>
                    <input type="number" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter donor's contact number" name="contact" value="<%=donor.contact%>">
                </div>
                <div class="mb-3">
                    <label class="block mb-1 text-sm">Address</label>
                    <input type="text" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter donor's address" name="address" value="<%=donor.address%>">
                </div>
                <div class="mb-3">
                    <label class="block mb-1 text-sm">Blood Group</label>
                    <input type="text" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Eg: AB+" name="blood_group" value="<%=donor.blood_group%>">
                </div>
                <div class="mb-6">
                    <label class="block mb-1 text-sm">Units Donated</label>
                    <input type="number" class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Eg: 100" name="units_donated" value="<%=donor.units_donated%>">
                </div>
                <button class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded">Update Donor</button>
            </form>
        </div>
    </div>
</body>

</html>